# BlackWave Environment Variables Example
# Copy this file to .env and fill in the required values.
# All variables are loaded by Docker Compose and both backend services.

# --- DATABASE CONFIGURATION ---
MYSQL_HOST=BlackWave-mysql           # Hostname for MySQL (use Docker service name or IP)
MYSQL_PORT=3306                      # MySQL port (default: 3306)
MYSQL_USER=blackwave                 # MySQL username
MYSQL_PASSWORD=blackwave_password    # MySQL password (choose a strong password)
MYSQL_DATABASE=blackwave_db          # MySQL database name

# --- QDRANT VECTOR DATABASE ---
QDRANT_HOST=BlackWave-qdrant         # Qdrant host (use Docker service name or IP)
QDRANT_PORT=6333                     # Qdrant port (default: 6333)

# --- INTERNAL SERVICE COMMUNICATION ---
SOCIAL_NETWORK_URL=http://BlackWave-social-network:8000  # Internal URL for Django backend (used by bot system)
API_KEY=your_internal_api_key        # Internal API key for secure communication between services (set to any random string, must match in both services)

# --- LLM PROVIDER API KEYS (at least one required for advanced bot intelligence) ---
OPENAI_API_KEY=                      # OpenAI API key (for GPT-3.5, GPT-4, etc.)
GOOGLE_API_KEY=                      # Google Gemini API key
OLLAMA_BASE_URL=                     # Ollama LLM base URL (e.g., http://localhost:11434)
OPENROUTER_API_KEY=                  # OpenRouter API key

# --- BOT SYSTEM CONFIGURATION ---
BOT_GROWTH_RATE=1.05                 # Audience growth multiplier per cycle (float > 1.0 for growth)
BOT_THEME=default                    # Bot avatar/bio theme (e.g., default, anime, cyberpunk)
BOT_MEMORY_LIMIT=100                 # Max number of memory entries per bot (integer)
BOT_COMMENT_PROB=0.2                 # Probability (0-1) that a bot comments on a post
BOT_LIKE_PROB=0.5                    # Probability (0-1) that a bot likes a post
BOT_DISLIKE_PROB=0.1                 # Probability (0-1) that a bot dislikes a post
BOT_REPOST_PROB=0.05                 # Probability (0-1) that a bot reposts a post
BOT_PERSONALITY_DISTR=fan:0.3,hater:0.1,neutral:0.4,humorous:0.1,provocative:0.1  # Distribution of bot personalities (comma-separated, format: type:probability)

# --- PUBLIC MODE ---
PUBLIC_MODE=false                    # If true, enables public read-only mode (only you can post, others can view)

# --- DJANGO SETTINGS ---
DJANGO_SECRET_KEY=your_django_secret_key   # Django secret key (required, set to a long random string)
DJANGO_DEBUG=False                   # Django debug mode (set to False in production)
ALLOWED_HOSTS=*                      # Comma-separated list of allowed hosts (use * for all)

# --- EMAIL SETTINGS (optional, for password reset/notifications) ---
EMAIL_HOST=                          # SMTP server host (optional)
EMAIL_PORT=                          # SMTP server port (optional)
EMAIL_HOST_USER=                     # SMTP username (optional)
EMAIL_HOST_PASSWORD=                 # SMTP password (optional)
EMAIL_USE_TLS=True                   # Use TLS for email (True/False)

# --- NOTES ---
# - At least one LLM API key (OPENAI_API_KEY, GOOGLE_API_KEY, OLLAMA_BASE_URL, or OPENROUTER_API_KEY) is required for advanced bot intelligence.
# - All variables are required unless marked optional. Defaults are provided where applicable.
# - Never share your .env file or API keys publicly.
# - For more details, see the README.md.
